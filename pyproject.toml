[project]
name = "thepurge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.0.0",
    "typer>=0.16.0",
]

[project.scripts]
the-purge = "src.main:main"